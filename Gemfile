source 'https://rubygems.org'
# This makes Bundler accept whatever Ruby version you're already running. Not
# recommended for general use, but helpful here where the only goal is to
# install the +rails+ gem.
ruby RUBY_VERSION
gem 'rails', '~> 7.0.0'
